{% extends 'base-admin.html.twig' %}

{% block body %}
    <section class="text-center container">
        <div class="row ">
            <div class="col-lg-6 col-md-8 mx-auto">
                <h1 class="fw-light">Übersicht der Unterstützung/Hilfe</h1>
            </div>
        </div>
        {% if service is empty %}
            {% include 'partials/no-data.html.twig' %}
        {% endif %}
    </section>
    <div class="album py-5">
        <div class="container">
            {% if service is not empty %}
                <table id="housing-table" class="table table-striped datatable" style="width:100%">
                    <thead>
                    <tr>
                        {% if is_granted('ROLE_ADMIN_SERVICE') %}
                            <th>Aktionen</th>
                        {% endif %}
                        <th>Unterstützung/Hilfe</th>
                        <th>benötigt</th>
                        <th>erstellt</th>
                        <th>aktualisiert</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for currentService in service %}
                        <tr>
                            {% if is_granted('ROLE_ADMIN_SERVICE') %}
                                <td>
                                    <div class="btn-group" role="group">
                                        <a href="{{ path('app_service_edit', {'id': currentService.id}) }}" type="button" class="btn btn-sm btn-warning"><i class="bi bi-pencil-square"></i></a>
                                        <a href="{{ path('app_service_remove', {'id': currentService.id}) }}" type="button" class="btn btn-sm btn-danger"><i class="bi bi-trash"></i></a>
                                    </div>
                                </td>
                            {% endif %}
                            <td>{{ currentService.title }}</td>
                            <td>{{ currentService.missingAmount }}</td>
                            <td>{{ currentService.created | date('d.m.Y H:m') }}</td>
                            <td>{{ currentService.updated | date('d.m.Y H:m') }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            {% endif %}
        </div>
    </div>
{% endblock %}
